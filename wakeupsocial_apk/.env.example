# For Flutter build, use --dart-define
# e.g.: flutter build apk --dart-define=SUPABASE_URL=... --dart-define=SUPABASE_ANON_KEY=...

# You can also use a tool like envied or flutter_dotenv if you prefer,
# but currently the app uses String.fromEnvironment().

SUPABASE_URL="YOUR_SUPABASE_URL"
SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"
